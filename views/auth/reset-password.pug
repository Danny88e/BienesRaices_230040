extends ../layout/index

block contenido 
    div.py-10
        h1.text-center.text-4xl.my-10.font-extrabold.text-black Bienes
            span.font-normal.text-am1-300  Raices
        h2.text-center.text-2xl.font-extrabold= page
        if errores
            each error in errores 
                div(class="tn-box tn-box-color-0 animate-fadeOut relative flex items-center")
                    img(src="/images/error.png", alt="" class="animate-fadeOut" width="50px")
                    p.text-center.ml-auto.mr-auto=error.msg
                    div(class="tn-progress animate-runProgress")
            script.
                const alertBoxes = document.querySelectorAll('.tn-box'); 
                alertBoxes.forEach(alertBox => {
                    alertBox.addEventListener('animationend', () => {
                    alertBox.style.display = 'none';
                    });
                });
        .mt-8.mx-auto.max-w-md 
            .bg-white.py-8.px-4.shadow 
                form(method="POST" action="/usuario/passwordRecovery/:token" noValidate).space-y-5.px-6
                    input(type="hidden" name="_csrf" value=csrfToken)
                    div
                        label.block.text-sm.uppercase.text-gray-500.mb-3.font-bold(for="new_password") Ingresa tu nueva contraseña: 
                        input#password.w-full.px-3.py-2.border.border-gray-300.rounded-md.placeholder-gray-400(placeholder="Contraseña" type="password" name="new_password")
                        label.block.text-sm.uppercase.text-gray-500.mb-3.font-bold(for="confirm_new_password") Confirma tu nueva contraseña:
                        input#confirm_new_password.w-full.px-3.py-2.border.border-gray-300.rounded-md.placeholder-gray-400(placeholder="Confirmar contraseña" type ="password" name="confirm_new_password")
                    .flex.items-center.justify-between
                        a.text-gray-500.text-xs(href="/usuario/createAccount" class="hover:underline hover:decoration-2 hover:decoration-am1-900") ¿No tienes cuenta? Crear cuenta
                        a.text-gray-500.text-xs(href="/usuario/login" class="hover:underline hover:decoration-2 hover:decoration-am1-900") ¿Ya tienes cuenta? Inicia Sesión 
                    
                    
                    input(class="w-full bg-am1-300 hover:bg-am1-400 text-white font-bold py-3 cursor-pointer text-center animate-fadedown" type="submit" value="Actualizar contraseña")